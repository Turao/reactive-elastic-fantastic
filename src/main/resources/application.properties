# Security
spring.security.user.name=admin
spring.security.user.password=admin

# Rabbit MQ
spring.rabbitmq.host=${RABBIT_HOST:localhost}

# Queues
receiver.queue.name=${receiverQueueName:myQueue}


# Actuator

# Persistence
spring.data.elasticsearch.client.reactive.endpoints=${ELASTICSEARCH_HOSTS:localhost}